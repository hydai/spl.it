import type { Suggestion, TLDData } from "../types";

export function findAbbreviationSuggestions(
  _input: string,
  _tldData: TLDData
): Suggestion[] {
  // Stub — will be implemented by strategy-ext agent
  return [];
}
